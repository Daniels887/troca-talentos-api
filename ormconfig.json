{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "postgres",
  "database": "troca_talentos",
  "entities": [
    "src/models/*.{ts,js}"
  ],
  "migrations": [
    "src/database/migrations/*.{ts,js}"
  ],
  "cli": {
    "migrationsDir": "src/database/migrations"
  }
}
